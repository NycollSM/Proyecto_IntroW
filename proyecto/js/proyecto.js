let formulario = document.getElementByName('formulario') [0];
let elementos = formulario.elements;
let boton = document.getElementById('comprar');

/** let validar_id= function(){
	if (formulario.direccion.value == 0){
		alert("Por favor completar el espacio de ID");
		e.preventDefault();
	}
}; 
 let validar = function(e){
 	validar_id(e);
 }
 formulario.addEventListener('comprar' , validar);
 **/

 /** Validar Categoria s
let categoria = function(){
	if(formulario.categoria[].checked==true);
	else{
		alert('completar categoria');
		e.preventDefault();
	}
}Validar Categoria**/